// WiFi:
const char* wifi_ssid = "<wifi-ssid>";
const char* wifi_secret = "<wifi-secret>";
int wifi_connect_retry_delay = 500; // Time to wait (in milliseconds) before retrying if connect to wifi failed. Defaults to 500.

/ MQTT Broker:
const char* mqtt_server = "<mqtt-broker-ip-or-hostname>";
const char* mqtt_topic = "<mqtt-topic>";
